# Project documents

Drop reference material here. Anything code needs to consume gets wired in
explicitly — agents read what's pointed at, not the entire folder.

Currently expected:

- `credit-check-api.{pdf,md}` — credit bureau API spec for Tenant registration.
- `house-rules.{pdf,docx,md}` — property house rules to surface during tenant
  onboarding.
- `lease-agreement-template.{pdf,docx}` — lease template signed at room
  assignment.
- `contractor-agreement-template.{pdf,docx}` — contractor service agreement.

Files committed here are reference-only — they don't ship to the deployed app.
